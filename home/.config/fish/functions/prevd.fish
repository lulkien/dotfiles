function prevd --description "Disable prevd from fish builtin"
  printf 'prevd: This function is disabled, use cd instead\n'
  printf 'cd -l ----------------- Show cd history\n'
  printf 'cd -i<i> -------------- cd to item <i>\n'
  printf 'cd --index=<index> ---- cd to item <index>\n'
end
