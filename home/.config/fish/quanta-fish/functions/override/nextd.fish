function nextd --description "Disable nextd from fish builtin"
  printf 'nextd: This function is disabled, use cd instead\n'
  printf 'cd -l ----------------- Show cd history\n'
  printf 'cd -i<i> -------------- cd to item <i>\n'
  printf 'cd --index=<index> ---- cd to item <index>\n'
end
