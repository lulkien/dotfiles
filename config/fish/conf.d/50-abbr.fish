# Abbreviations
abbr -a chmox 'chmod +x'
abbr -a rf 'rm -rfi'

# Misc
abbr -a ipa 'ip address'

# Cargo
abbr -a cbr 'cargo build --release'
abbr -a crr 'cargo run --release'

abbr -a pls sudo
abbr -a cls clear
abbr -a q exit
