# Format: <config>|<operation>|<destination>
# <destination> is the part after $HOME
# <destination> can be empty, default destination will be used: $HOME/.config/<config>

fish|symlink|
bash|symlink|
nvim|symlink|
vim|symlink|.vim
