(defpoll TIME :interval "2s"
    "date '+%A, %b %d | %R'")

(defwidget date-time []
   (box :class "date-time bar-container"
        :orientation "h"
        :space-evenly false
        (label
            :text "${TIME}"
        )
   )
)
